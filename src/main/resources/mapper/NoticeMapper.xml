<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.medusa.basemall.shop.dao.NoticeMapper">
    <resultMap id="BaseResultMap" type="com.medusa.basemall.shop.entity.Notice">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="notice_id" jdbcType="INTEGER" property="noticeId"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="notice_img" jdbcType="VARCHAR" property="noticeImg"/>
        <result column="create_time" jdbcType="VARCHAR" property="createTime"/>
        <result column="product_id" jdbcType="BIGINT" property="productId"/>
        <result column="target_name" jdbcType="VARCHAR" property="targetName"/>
        <result column="appmodel_id" jdbcType="VARCHAR" property="appmodelId"/>
        <result column="target_url" jdbcType="VARCHAR" property="targetUrl"/>
        <result column="notice_body" jdbcType="LONGVARCHAR" property="noticeBody"/>
        <!--<result column="sort" jdbcType="INTEGER" property="sort"/>-->
        <!--<result column="start_time" jdbcType="VARCHAR" property="startTime"/>-->
        <!--<result column="end_time" jdbcType="VARCHAR" property="endTime"/>-->
    </resultMap>

    <sql id="Base_Column_List">
        notice_id, title, notice_img, create_time, product_id, target_name, appmodel_id, target_url, notice_body
    </sql>


    <update id="cleanProduct" parameterType="list">
        update t_notice set product_id = NULL , target_name =NULL
        where product_id
        <foreach collection="productIds" item="productId" open="in(" close=")" separator=",">
            #{productId}
        </foreach>
    </update>

    <select id="selectByAppmodelId" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        from t_notice
        where appmodel_id = #{appmodelId,jdbcType=VARCHAR} order by create_time desc
    </select>

    <delete id="batchDelete" parameterType="java.lang.String">
        delete from t_notice
        where notice_id in
        <foreach item="noticeId" index="index" collection="array" open="("
                 separator="," close=")">
            #{noticeId}
        </foreach>
    </delete>

</mapper>